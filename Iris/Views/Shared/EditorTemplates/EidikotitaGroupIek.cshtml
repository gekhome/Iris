@using System.Collections
@using Iris.Models
@using Iris.DAL
@model int

@(Html.Kendo().DropDownListFor(m => m)
    .AutoBind(true)
    .DataValueField("EEGROUP_ID")
    .DataTextField("EEGROUP_TEXT")
    .OptionLabel("Επιλογή...")
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("GroupsIekRead", "Admin2");
        });
    })
)
