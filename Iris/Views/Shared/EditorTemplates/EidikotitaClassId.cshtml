@using System.Collections
@using Iris.Models
@using Iris.DAL
@model object

@(Html.Kendo().DropDownListFor(m => m)
    .AutoBind(true)
    .DataValueField("CLASS_ID")
    .DataTextField("CLASS_TEXT")
    .OptionLabel("Επιλογή...")
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("ClassesRead", "Admin2");
        });
    })
)
